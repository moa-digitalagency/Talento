# ============================================
# TALENTO - Configuration de l'environnement
# ============================================
# IMPORTANT: Copiez ce fichier vers .env et remplissez avec vos vraies valeurs
# Ne jamais committer le fichier .env dans Git !

# ============================================
# 1. CLÉS DE SÉCURITÉ (OBLIGATOIRES)
# ============================================

# Clé secrète Flask - Générez une avec:
# python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your-secret-key-here-generate-a-random-one

# Clé de chiffrement pour les données sensibles (téléphone, adresse, etc.)
# Générez une avec: python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
# ⚠️ TRÈS IMPORTANT: Ne changez JAMAIS cette clé après avoir créé des utilisateurs
# Si vous la changez, toutes les données chiffrées (téléphones, adresses) seront perdues !
ENCRYPTION_KEY=your-encryption-key-here-generate-a-random-one

# Mot de passe du compte admin (Email: admin@talento.com, Code: MAN0001RAB)
# Choisissez un mot de passe fort et sécurisé
ADMIN_PASSWORD=your-strong-admin-password-here

# ============================================
# 2. BASE DE DONNÉES
# ============================================

# PostgreSQL (Production recommandée):
# DATABASE_URL=postgresql://username:password@localhost:5432/talento_db

# SQLite (Développement local):
DATABASE_URL=sqlite:///talento.db

# ============================================
# 3. URL DE BASE (Pour QR codes et liens publics)
# ============================================

# Production VPS:
# BASE_URL=https://talentsmaroc.com

# Replit (auto-détecté):
# (Laissez vide sur Replit, sera auto-détecté via REPLIT_DOMAINS)

# Développement local:
# BASE_URL=http://localhost:5000

# ============================================
# 4. CONFIGURATION EMAIL (Optionnel)
# ============================================

# Option A: SMTP Gmail
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-gmail-app-password
# MAIL_DEFAULT_SENDER=your-email@gmail.com

# Option B: SendGrid (recommandé pour production)
# SENDGRID_API_KEY=your-sendgrid-api-key-here

# ============================================
# 5. APIS EXTERNES (Optionnel)
# ============================================

# OpenRouter (pour l'analyse de CV avec IA)
# OPENROUTER_API_KEY=your-openrouter-api-key

# OMDB (pour les informations de films)
# OMDB_API_KEY=your-omdb-api-key

# ============================================
# 6. AUTRES PARAMÈTRES
# ============================================

# Environnement (development / production)
FLASK_ENV=development

# Active l'initialisation automatique de la base de données au premier démarrage
ENABLE_AUTO_SEED=1

# ============================================
# NOTES IMPORTANTES:
# ============================================
#
# 1. SECRET_KEY: Doit être unique et secret. Changez-la régulièrement pour plus de sécurité.
#
# 2. ENCRYPTION_KEY: ⚠️ NE JAMAIS CHANGER après la création des premiers utilisateurs !
#    Si vous la changez, toutes les données chiffrées (téléphones, adresses, etc.) 
#    deviendront irrécupérables. Sauvegardez-la en lieu sûr !
#
# 3. ADMIN_PASSWORD: Choisissez un mot de passe fort pour le compte admin.
#    Vous pouvez le changer après la première connexion.
#
# 4. Pour générer les clés:
#    - SECRET_KEY: python -c 'import secrets; print(secrets.token_hex(32))'
#    - ENCRYPTION_KEY: python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
#
# 5. Sur VPS avec nginx, assurez-vous que BASE_URL pointe vers votre domaine HTTPS
#
